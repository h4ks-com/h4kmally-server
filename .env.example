## h4kmally server config
## Mass = Size² / 100  (size 100 = mass 100)

# Map (half-width: total map is MAP_WIDTH*2 x MAP_HEIGHT*2)
MAP_WIDTH=7071
MAP_HEIGHT=7071

# Player
START_SIZE=100
MIN_PLAYER_SIZE=100
MIN_SPLIT_SIZE=122.5
MAX_CELLS=16
DECAY_RATE=0.9998
DECAY_MIN_SIZE=100
EJECT_SIZE=36.06

# Food
FOOD_COUNT=30000
FOOD_SIZE=17.3
FOOD_SPAWN_PER=150

# Virus
VIRUS_COUNT=30
VIRUS_SIZE=141.4

# Auth (Logto OAuth2) — REQUIRED
LOGTO_ENDPOINT=https://auth.example.com
SUPER_ADMIN=

# Server
PORT=3001
