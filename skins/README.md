# Skins Directory

Skin images are managed through the admin panel. Upload skins via the web interface — they'll be stored here alongside `manifest.json`.

Supported formats: PNG, JPG, GIF, WebP.

The `manifest.json` file tracks all uploaded skins and their metadata (name, file, category, rarity, min_level).
